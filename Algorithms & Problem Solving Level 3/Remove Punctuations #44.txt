string ReadName()
{
	string word;
	cout << "enter the name : " << endl;
	getline(cin, word);

	return word;
}

string RemovePunc(string word)
{
	string word2 = "";

	for (int i = 0; i < word.length(); i++)
	{
		if (!ispunct(word[i]))
		{
			word2 += word[i];
		}
	}
	return word2;
}




int main()
{
	string word = ReadName();
	cout << "after remove punct : " << RemovePunc(word) << endl;
