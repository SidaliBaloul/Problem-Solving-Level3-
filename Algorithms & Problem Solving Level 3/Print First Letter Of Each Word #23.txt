string ReadName()
{
	string name;
	cout << "please enter ur name : " << endl;
	getline(cin, name);

	return name;
}
void PrintName(string name)
{
	int na = name.length();

	bool isfirst = true;

	for (int i = 0; i < na; i++)
	{
		if (name[i] != ' ' && isfirst)
		{
			cout << name[i] << endl;
		}
		isfirst = (name[i] == ' ' ? true : false);
	}
}

int main()
{

	
	PrintName(ReadName());
